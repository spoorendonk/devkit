Update dev-standards to the latest version and re-apply all shared files.

Run this command:

```bash
bash .dev-standards/update.sh
```

After the update completes, review what changed:

```bash
git diff
git status
```

Summarize what was updated. If settings.json changed, highlight new permissions or hooks that were added.
